Script to enumerate AD DHCP scopes.
Extract the reservations for those scopes.
Compare MAC addresses to the DHCP Filter.

[todo]
Add missing MAC addresses to Allow filter.
Takes some time to run - so needs tuning.
